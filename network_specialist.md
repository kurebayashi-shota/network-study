# DNS

- 名前解決=UDP:53,ゾーン転送=TCP:53
- 再帰問い合わせ: クライアント->キャッシュ
- 反復問い合わせ: キャッシュ->ルート->TLD 権威->権威
- URL(スキーマ+FQDN(ホスト+ドメイン))
- ゾーン転送:権威 DNS がマスターとスレーブで同期

```
ゾーン情報に更新があった
1. [マスター]--notify:UDP-->[スレーブ]
2. [マスター]<--下さい:TCP--[スレーブ]

SOA レコードのリフレッシュ間隔時間に更新がなかった場合
1. [マスター]<--更新ない?--[スレーブ]
2. [マスター]--ないよ-->[スレーブ]
```
TLS証明書の種類
- DV（ドメイン認証）
- OV（組織認証）
- EV（拡張認証）

| A    | AAAA | MX     | CNAME | NS     | PTR    | TXT  |
| ---- | ---- | ------ | ----- | ------ | ------ | ---- |
| IPv4 | IPv6 | メール | 別名  | ゾーン | 逆引き | 自由 |

- DKIM,DMARK,SPF レコードは TXT で管理
- SOA レコード:マスター(ゾーン管理)とスレーブ(差分)で管理内容が違う

---

# メール

- M(U,S,T,T,D,R)A
- SPF:送信ドメインの名前解決をして IP を確認
- DKIM:証明書で確認
- DMARK:SPF や DKIM ではじかれたメールはどうするか決める
- POP3:110,995 取り出す
- IMAP:143,993 サーバーにあるのを見に行く

## SMTP:25,465 送信

- [EHLO]->[AUTH(任意)]->[MAIL_FROM]->[RCPT_TO]->[DATA]->[QUIT]
- SMTP_AUTH:ID と pass で認証
- POPbeforeSMTP:受信サーバーで送信前に認証
- サブミッションポート 587:他 ISP 管理のメールサーバーへの送信時に認証後に送信するポート

---

# FTP(21 20 アクティブの場合はデータ転送)

| アクティブ | パッシブ |
| ---------- | -------- |
| PORT       | PASV     |
| cl<-sv     | cl->sv   |

- FW があればパッシブを選択
- ACCT:アカウント
- MODE:モード選択
- WebDAV:http を使って FTP+編集 ができる仕組み。FTP の方が高速

---

# SNMP:161

- エージェント
- マネージャー
- ポーリング:マネージャーから確認
- トラップ:エージェントから通知
- MIB:エージェント情報
- インフォーム:再送制御

---

# 負荷分散

## DNS ラウンドロビン

- DNS サーバーの設定のみで可能だが、非 activeSV にも振り分けられてしまう

## LB

- ワンアーム構成:既存に腕をつけるみたいに横づけで追加する
- インライン構成:既存の上位または入れ替えて追加する
- ソース NAT:同一ネットワークにある場合で送信元 IP を LB に変換する
- DSR:宛先仮想 IP はそのままで物理アドレスを宛先サーバーにする

### 振り分け方式

- ラウンドロビン
- 最小接続
- 最小負荷

---

# セキュリティ

## FW

- ステートフルフェイルオーバー:設定、動作情報の引き継ぎ
- ステートフルインスペクション(検査):行きの情報を基に帰りも許可する仕組み
- FW=L4 まで、WAF=L5 以上

## 検知・通知

| IDS            | IPS          | シグネチャ型   | アノマリ       |
| -------------- | ------------ | -------------- | -------------- |
| 不正な挙動検知 | IDS+ブロック | 不正動作で検知 | 正常動作で検知 |

- ミラーポート:監視対象ポートと同じ通信を流す

---

# STP

- Dws
- ディブリラフ(ディスカーディング)
- ルート、指定、ブロッキング(代替、バックアップ)
- BPDU を定期的にやりとりしてノードの状態を管理
- RSTP:コンバージェンスが早い
- MSTP:VLAN 毎にグループ(MSTI)を作れる
- TRILL:STP ではないけど進化系

---

# MPLS:LESER_pushswappop

- IP-VPN で使う
- L(E,S,E)R
- PUSH,SWAP,POP
- IPv4,6 両方で使える

---

# IP-PBX:ビジネスフォン

- IP-PBX で SIP は張るだけ
- ルータ:データを受け取って普通の電話ならゲートウェイに変換してもらう
- PBX:普通の電話<->PBX<->ゲートウェイに変換してもらうという振り分け
- VoIP ゲートウェイ:音声<->IP を変換
- VoIP ゲートキーパ:IP 電話の司令塔(SIP サーバーがあれば不要)
- ネットワーク-ルーター-VoIPgw-PBX-電話
- クラウド PBX もある

---

# IEEE802.1X 認証

- Wep キーと比べて乱数を基にセッション毎で鍵を生成=セキュリティ強度が高い
- PKI(公開暗号基盤)

## EAP-TLS フロー

1. **EAPOL 開始**
   [Supplicant] ------------------> [Authenticator]

- 認証したいのでサーバーさんへ通してください
  (ネットワークアクセス開始)

2. **EAP 要求/Identity**
   [Supplicant] <------------------ [Authenticator]

- ID 教えて
  (サプリカントに ID 要求)

3. **EAP 応答/Identity**
   [Supplicant] ------------------> [Authenticator]

- 私の ID です。  
  (ユーザー名/端末 ID 送信)

4. **RADIUS Access-Request (EAP Identity)**
   [Authenticator] ------------------> [AuthenticationServer]

- サーバーさんこの ID の認証方式を教えて
  (EAP Identity を RADIUS で転送)

5. **RADIUS Access-Challenge (EAP-TLS)**
   [Authenticator] <------------------ [AuthenticationServer]

- TLS です
  (TLS ベースの認証方式指示)

6. **証明書をどうぞ:往復**
   [Supplicant] <--> [Authenticator] <--> [AuthenticationServer]

- サーバ証明書提示
- サプリカント証明書送信

7. **PMK 生成用の乱数をどうぞ:往復**
   [Supplicant] <--> [Authenticator] <--> [AuthenticationServer]

- それを基に互いに PTK(共通鍵)を生成
- IKEv1 に似てる

8. **EAP-TLS Handshake**
   [Supplicant] <--> [Authenticator] <--> [AuthenticationServer]

- 4way ハンドシェイク(TLS ハンドシェイク開始)

  - 鍵交換・暗号化トンネル確立

9. **EAP-TLS Success / RADIUS Access-Accept**
   [Supplicant] <-- [Authenticator] <-- [AuthenticationServer]

- 鍵作ったから通っていいよ  
   (認証成功、ネットワークアクセス許可)

10. **ネットワークアクセス開始**  
    [Supplicant] ------------------> ネットワーク

## AD との関わり

- AD:アクセス権限のユーザー情報を管理
- LDAP(636):AD 情報自体を運ぶ
- kerberos:AD 情報を基に許可証を発行して持っていく

---

# VPN

## IPsec

トランスポートモードとトンネルモードがある

- NAT トラバーサル:NAPT 機器が間にあると L4 もカプセル化してしまうのでポートが分からなくなるので UDP でカプセル化する
- GREoverIPsec:ユニキャストの IPsec 通信を受信ルーターまで持って行って受信側でマルチキャストにしてくれる

---

# ハブ&スポーク構成

- ハブルーター=親
- スポークルーター=子供
- スポークルーターはハブルーター経由

- NHRP:ハブルーターへはルーティング情報だけもらいに行ける

---

# データ

| L2       | L3       | L4         |
| -------- | -------- | ---------- |
| フレーム | パケット | セグメント |

# L7

- WebSocket:双方向通信だがクライアントからの GET リクエストヘッダー内の connection に upgrade を入れると開始できる

## DHCP

UDP・IP・イーサネットの順でカプセル化される

- DHCP リレーエージェント:ルーターを超えれないので R がサーバー当てにユニキャストする

1. **DHCP ディカバー**

- 誰か IP アドレスをください
  [Client] ----> [Server]

2. **DHCP オファー**

- この IP でどうでしょう?
  [Client] <---- [Server]

3. **DHCP リクエスト**

- じゃぁそれにします。
  [Client] ----> [Server]

4. **DHCP アック**

- 登録しときますね  
   [Client] <---- [Server]

# L6

- TLS ハンドシェイク(Cl ハロー->Sv ハロー->Sv サーティフィケート)
- SSL:(DOE)V DV=簡単,OV=所在確認,EV=業務内容も見る

# L4

- UDP:送信元,宛先,長さ,チェックサム
- TCP:UDP+制御ビット+確認応答番号+シーケンス番号+ウインドウサイズ
- RTP:UDP+ペイロードに RTP 情報を組み込んで着順を管理

# L3

- IGP:RIP,OSPF
- EGP:BGP

## IPv4

- ホストアドレス計算:a&~m
- 2 の 32 乗
- 127:ループバック
- 224-239(クラス D):マルチキャストアドレス
- IGMP

## IPv6

- サブネットプレフィックス+インターフェース ID
- 2 の 128 乗
- Mac アドレスが見える(EUI-64):IPv6 Privacy Extensions で解決(RFC4941)

| ユニキャスト        | マルチキャスト           | エニーキャスト                 |
| ------------------- | ------------------------ | ------------------------------ |
| リンクローカル fe   | ブロードキャストの代わり | 同 GP の直近ノード一か所に送る |
| ユニークローカル fc |
| グローバルユニ 2000 |

# L2

## MAC アドレス

- OUI+NicID

## VLAN:IEEE802.1Q

- ポート:ポートに VLAN 番号を付与
- タグ:フレームに VLAN 番号を付与
- CoS:VLAN タグに PCP を埋め込み優先度管理
- DSCP:WAN でも使える CoS みたいなの

# L1

## LAN

- T:ツイストペアケーブル
- SL:光ファイバー(シングル、マルチ)
- ピン配列:MDI/MDIX
- PoE(802.3af, 802.3at)
- PLC:電力線でデータ通信

### 有線

| 3   | 4   | 5   | 5e   | 6   | 7   |
| --- | --- | --- | ---- | --- | --- |
| 100 | 100 | 100 | 1000 | 10G | 10G |

### 無線

| b(2) | a(2) | g(3) | n(4) | ac(5) | ax(6) |
| ---- | ---- | ---- | ---- | ----- | ----- |
| 11M  | 54M  | 54M  | 600M | 6.9G  | 9.6G  |

| WEP | WPA | WPA2 |
| --- | --- | ---- |
| RC4 | RC4 | AES  |

- フレームアグリゲーション:複数フレームをまとめて伝送
- チャネルボンディング:2 チャネルをまとめて使える
- MIMO:複数アンテナでデータをバラバラにして送受信
- アソシエーション:接続プロセス。無線 LAN ではポートとして扱う

## LAG

- リンクアグリゲーション
- マルチシャーシ(スタック接続)

---

# QoS

- ネットワーク上の通信品質を制御するために、通信の種類ごとに優先度・帯域・遅延・パケット損失などを管理・制御する仕組み
- MQTT では最大 1 回・最小 1 回・確実に 1 回の三段階で管理。パブリッシャー->ブローカー->サブスクライバー

1. 方式決定(DiffServ もしくは IntServ)
2. 分類（階層構造で管理）
3. マーキング（DSCP など）
4. 制御（トラフィック制御技術+必要なら CoS）

## ■ 方式

### 1. DiffServ（Differentiated Services）:優先順位ベースの制御（主流）

通信をクラス分けして優先制御する方式。

- パケットの IP ヘッダに DSCP 値を付与して識別
- ルータはその値を元に処理を変更

### 2. IntServ（Integrated Services）:現在はあまり使われない

通信ごとに帯域を事前予約する方式。

## ■ トラフィック制御技術

### ポリシング（Policing）:超過分を破棄

通信量が上限を超えた場合に制限する仕組み。破棄する

### シェーピング（Shaping）:超過分を遅延

通信量をなだらかに調整する仕組み。破棄しない

### キューイング（Queuing）

キューにいれて順番待ち制御。

### スケジューリング（Scheduling）

どのキューからパケットを送信するか決定する仕組み。

### アドミッション制御

IntServ セットで使われる

---

# その他

- MQTT:IoT 向けプロトコルで 1:多の信頼性の高いメッセージ配信
- M2M:物と物を繋ぐ技術
- ZigBee:物と人を繋ぐ低速で短距離だが省電力の IoT 向けのプロトコル
- CASB:クラウド利用の監視・制御・保護の概念やソフトウェア
- SOAP:XML でデータをやり取りする API のプロトコルで http/s で動作。
- SAML:XML で認証+属性・アクセス情報を別ドメインの Web サービスプロトコルを OASIS が定めたもの
- SIEM:SV、NW 機器等のログを一元的に収集・分析、検知して通知する
- ATM:分割して送信するので LAN が高速化
- SAN(高価:FC-,安価:IP-)
- uRPF:ルーターが送信元 IP を見て破棄の設定。ルーズ OR ストリクト
- SDN:ネットワークをソフトウェアで一括管理する考え
- OpenFlow:SDN を実現する為のプロトコル
- Web 無害化:<mark>リスク削除</mark>(js 等を mpeg4 に変換) or <mark>画像転送</mark>(画像にして Cl に渡す)
- RSS:ニュースやブログ等の最新情報をまとめてくれる技術。
- OAuth2.0:SSO の仕組み。

---

# 攻撃

- DHCP スヌーピング:DHCP をのぞき見る
- DHCP キャッシュポイズニング:DHCP キャッシュ情報を偽 IP に変更させる
- カミンスキー攻撃:存在しないドメインの名前解決を要求して ↑ をする
- ARP スプーフィング: ARP テーブルを汚染してこっそり MITM になる。
  通信制御装置を MITM にして防御する事も出来る
- IP スプーフィング:送信元 IP を偽装する
- DNSamp 攻撃:IP スプーフィングをして bot ネットに DNS 問い合わせさせる
- スマーフ攻撃:IP スプーフィングをしてブロードキャストで ICMP
- プードル攻撃:SSL3 の脆弱性をつく攻撃
- ビースト攻撃:TLS1.1 の脆弱性をつく攻撃
- RLO 攻撃:右から左にオーバーライドして偽装
- Syn フラッド攻撃:Syn を送り続けて Syn/Ack に対して Ack を返さない
- mirai:IoT に感染して DDoS 攻撃させる
- FastFlacks:悪サイトの IP を BotNetIP に高速に入れ替えて発見にくくする。
- MITM 攻撃(Middle)：通信の途中に割り込んで盗聴・改ざんする攻撃
- MITB 攻撃(Browser)：ブラウザ内にマルウェアが潜んでユーザー操作や通信内容を密かに改ざんする攻撃

---
